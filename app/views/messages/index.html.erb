<div class="container">
  <h1>Messages page</h1>
  <button class="btn btn-success">New conversation</button>
  <% if @elements %>
  <% @elements.each do |element| %>
    <div class="col-lg-3">
    <%= element.id %>
    </div>
  <% end %>
  <% end %>
</div>