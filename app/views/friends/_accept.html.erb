<%= form_with(model: friend, url: friends_accept_path, remote: true, method: :post ) do |f| %>
    <%= f.hidden_field :id, value: friend.id %>
    <%= f.submit "Accept", data: { disable_with: false }, class: "btn btn-success" %>
<% end %>