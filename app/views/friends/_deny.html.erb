<%= form_with(model: friend, url: friends_deny_path, remote: true, method: :post ) do |f| %>
    <%= f.hidden_field :id, value: friend.id %>
    <%= f.submit defined?(btntext) ? btntext : "Deny", data: { disable_with: false }, class: defined?(btnclass) ? btnclass : "btn btn-danger" %>
<% end %>