<h1>Users</h1>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user %></li>
    (<%= link_to "Add Contact", add_contact_path(recipient_id: user.id), method: :post %>)
  <% end %>
</ul>
